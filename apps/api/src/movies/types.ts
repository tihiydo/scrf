export type QueryFilters = {
  sortBy: 'imdb_rating' | 'by_popularity' | 'by_novelty';
  year: string;
  rating: string;
  genre: string;
  page: string;
  take: string;
  audio: string;
  subtitles: string;
  studio: string;
};
